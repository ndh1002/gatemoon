Optional XGBoost model file: place `moonshot_xgb.json` here (native XGBoost JSON model).

Train with your own labeled dataset using `scripts/train_xgb.py` (see script docstring). If the file is absent, the API uses the built-in weighted ensemble.
